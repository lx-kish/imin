import React from "react";

import "./host-event.styles.scss";

const HostEvent = (props) => {
	/**
   * HostEvent page 
   * 
   * Contains menu choise for hosting new event or based on previous event settings.
   * 
   */

	return (
    <div>
      Host Event:
      Contains menu choise for hosting new event or based on previous event settings
    </div>
  );
};

export default HostEvent;
