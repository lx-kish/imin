import React from "react";

import "./settings.styles.scss";

const Settings = (props) => {
	/**
   * settings page 
   * 
   * Contains application, account, security, and other settings.
   * 
   */

	return (
    <div>
      Settings
    </div>
  );
};

export default Settings;
