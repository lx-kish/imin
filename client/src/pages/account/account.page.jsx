import React from "react";

import "./account.styles.scss";

const Account = (props) => {
	/**
   * Account page 
   * 
   * Contains email and password as an account details.
   * 
   */

	return (
    <div>
      accout
    </div>
  );
};

export default Account;
