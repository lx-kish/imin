const request = require('supertest');
const express = require('express');

const app = express();
const auth = require('../server/server');

// const chai = require('chai');
// const expect = chai.expect();